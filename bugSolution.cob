MOVE SPACES TO WS-OUTPUT-AREA.

IF WS-DIVISOR = 0 THEN
    MOVE "Division by zero error" TO WS-OUTPUT-AREA
ELSE
    COMPUTE WS-RESULT = WS-INPUT-DATA / WS-DIVISOR
    MOVE WS-RESULT TO WS-OUTPUT-AREA
END-IF.

DISPLAY "Result:" WS-OUTPUT-AREA.